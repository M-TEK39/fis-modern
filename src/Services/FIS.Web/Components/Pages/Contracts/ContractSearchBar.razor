@using Microsoft.AspNetCore.Components.Web

<div class="search-bar">
    <div class="search-input-wrapper">
        <i class="bx bx-search"></i>
        <input 
            type="text" 
            class="search-input" 
            placeholder="Search contracts by vehicle, department, or contract number..." 
            @bind="SearchTerm" 
            @oninput="OnSearchInput" />
        @if (!string.IsNullOrWhiteSpace(SearchTerm))
        {
            <button class="search-clear" @onclick="ClearSearch" aria-label="Clear search">
               <i class="bx bx-x" style="margin-left:-12px;"></i>
            </button>
        }
    </div>
</div>

@code {
    [Parameter] public string SearchTerm { get; set; } = "";
    [Parameter] public EventCallback<string> OnSearchChange { get; set; }

    private async Task OnSearchInput(ChangeEventArgs e)
    {
        SearchTerm = e.Value?.ToString() ?? "";
        await OnSearchChange.InvokeAsync(SearchTerm);
    }

    private async Task ClearSearch()
    {
        SearchTerm = "";
        await OnSearchChange.InvokeAsync(SearchTerm);
    }
}
@page "/job-cards/capturer-default"
@rendermode InteractiveServer
@using FIS.Web.Components.Shared

<LegacyReportEndpointPage
    Title="Job Cards Main Menu"
    Description="Capture, edit, print, cancel, and close jobcards from the legacy capturer view."
    LegacyPath="Capture, edit, print, cancel, and close jobcards from the legacy capturer view."
    Endpoint="api/jobcards/priority/unassigned"
    HttpMethod="GET"
    ReportType="jobcards-priority-unassigned"
    BackLink="/job-cards"
    Icon="bx-wrench" />

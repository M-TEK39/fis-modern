@page "/full-maintenance-lease/upload"
@rendermode InteractiveServer
@using FIS.Web.Components.Shared

<LegacyReportEndpointPage
    Title="Upload the Lease Tariff File"
    Description="Import the lease tariff file with the legacy format."
    LegacyPath="Import the lease tariff file with the legacy format."
    Endpoint="api/leasecontractterms/import"
    HttpMethod="POST"
    ReportType="fml-upload-tariffs"
    BackLink="/full-maintenance-lease"
    Icon="bx-wrench" />

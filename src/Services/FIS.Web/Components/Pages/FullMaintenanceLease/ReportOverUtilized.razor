@page "/full-maintenance-lease/reports/over-utilized"
@rendermode InteractiveServer
@using FIS.Web.Components.Shared

<LegacyReportEndpointPage
    Title="List of over utilized FML vehicles (based on km's)"
    Description="Legacy fleet operations endpoint wiring page."
    LegacyPath="Legacy fleet operations endpoint wiring page."
    Endpoint="api/leasecontractterms/reports/over-utilized"
    HttpMethod="POST"
    ReportType="fml-report-over-utilized"
    BackLink="/full-maintenance-lease/reports"
    Icon="bx-wrench" />

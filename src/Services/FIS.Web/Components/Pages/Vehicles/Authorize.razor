@page "/vehicles/authorize"
@rendermode InteractiveServer
@using FIS.Web.Components.Shared

<LegacyReportEndpointPage
    Title="Authorize Captured Vehicle Master Information"
    Description="Legacy authorization workflow (UI-only until endpoints are available)."
    LegacyPath="Legacy authorization workflow (UI-only until endpoints are available)."
    Endpoint="api/vehicle-inception/awaiting-authorization"
    HttpMethod="GET"
    ReportType="vehicle-inception-awaiting"
    BackLink="/vehicles"
    Icon="bx-wrench" />

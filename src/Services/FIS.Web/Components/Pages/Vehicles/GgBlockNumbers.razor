@page "/vehicles/gg-block-numbers"
@rendermode InteractiveServer
@using FIS.Web.Components.Shared

<LegacyReportEndpointPage
    Title="Add GG Block Number(s)"
    Description="Legacy GG block numbering screen (UI-only until API endpoints are available)."
    LegacyPath="Legacy GG block numbering screen (UI-only until API endpoints are available)."
    Endpoint="api/vehicle-inception/gg-blocks"
    HttpMethod="POST"
    ReportType="vehicle-inception-gg-blocks"
    BackLink="/vehicles"
    Icon="bx-wrench" />

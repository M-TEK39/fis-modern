@page "/vehicles/status-maintenance"
@rendermode InteractiveServer
@using FIS.Web.Components.Shared

<LegacyReportEndpointPage
    Title="Vehicle Status Maintenance"
    Description="Select the vehicle for status management (UI-only until API endpoints are available)."
    LegacyPath="Select the vehicle for status management (UI-only until API endpoints are available)."
    Endpoint="api/vehicles/status-maintenance/search?query="
    HttpMethod="GET"
    ReportType="vehicle-status-maintenance"
    BackLink="/vehicles"
    Icon="bx-wrench" />

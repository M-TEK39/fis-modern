@page "/vehicles/recovered"
@rendermode InteractiveServer
@using FIS.Web.Components.Shared

<LegacyReportEndpointPage
    Title="Update Recovered GG"
    Description="Legacy recovered vehicle update (UI-only until API endpoints are available)."
    LegacyPath="Legacy recovered vehicle update (UI-only until API endpoints are available)."
    Endpoint="api/vehicles/recovered-gg"
    HttpMethod="POST"
    ReportType="vehicles-recovered-gg"
    BackLink="/vehicles"
    Icon="bx-wrench" />

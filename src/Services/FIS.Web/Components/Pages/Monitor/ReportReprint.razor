@page "/monitor/reports/reprint"
@rendermode InteractiveServer
@using FIS.Web.Models
@using FIS.Web.Services
@inject MonitorApiService MonitorService
@inject IJSRuntime JSRuntime

<PageTitle>Reprint an Inquiry Report - Fleet Information System</PageTitle>

<div class="page-header">
    <div class="page-header-content">
        <h1 class="page-title">
            <i class="bx bx-printer"></i>
            Reprint an Inquiry Report
        </h1>
        <p class="page-description">Find and reprint a captured inquiry using its reference number.</p>
    </div>
</div>

<div class="page-content">
    <div class="form-card">
        <div class="form-row">
            <label class="form-label">Reference Number</label>
            <input class="form-input" @bind="_referenceNumber" />
        </div>
        <div class="form-actions">
            <button class="btn btn-primary" @onclick="LoadInquiry">Submit</button>
        </div>
    </div>

    @if (_loading)
    {
        <div class="loading-state">
            <div class="spinner"></div>
            <p>Loading...</p>
        </div>
    }
    else if (_hasSearched && _result == null)
    {
        <div class="empty-state">
            <i class="bx bx-printer"></i>
            <h3>No inquiry found</h3>
            <p>Enter a reference number to reprint.</p>
        </div>
    }
    else if (_result is not null)
    {
        <div class="table-container">
            <div class="table-wrapper">
                <table class="data-table">
                    <tbody>
                        <tr><th>Reference</th><td>@_result.monitor_code</td></tr>
                        <tr><th>Inquiry Type</th><td>@_result.Inquiry_type</td></tr>
                        <tr><th>Description</th><td>@_result.Inquiry_Desc</td></tr>
                    </tbody>
                </table>
            </div>
        </div>
    }
    else
    {
        <div class="empty-state">
            <i class="bx bx-printer"></i>
            <h3>Enter reference number</h3>
            <p>Submit a reference number to reprint an inquiry.</p>
        </div>
    }
</div>

@code {
    private bool _loading;
    private bool _hasSearched;
    private string _referenceNumber = string.Empty;
    private MonitorDto? _result;

    private async Task LoadInquiry()
    {
        _loading = true;
        _hasSearched = true;
        _result = null;
        try
        {
            if (short.TryParse(_referenceNumber, out var code))
            {
                _result = await MonitorService.GetByIdAsync<MonitorDto>(code);
            }
        }
        catch (Exception ex)
        {
            await JSRuntime.InvokeVoidAsync("console.error", ex.Message);
        }
        finally
        {
            _loading = false;
        }
    }
}
